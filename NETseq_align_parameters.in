##Parameters

# provide the base directory where every file will be created; it could consist of a whole path
Base Directory : /home/NETseq/X

# provide the sample(s) name(s); if more than 1, separate them by "\n" without spaces  (for example X_S1.fastq and X_S2.fastq): 
Sample Names : X_S1\nX_S2

# provide an email adress, so that you will get job notifications
Notification Email : foo@bar.edu

# provide the directory of the genome index that will be used by the STAR aligner
Index Directory : /home/genomes/hg38/STARindex/

# provide the directory of the raw fastq files (if there are several samples they need to be in the same directory)
Initial Files Directory: /home/NETseq/X/startingfiles/

# provide the directory containing all scripts and the parameter file
Scripts Directory : /home/code


